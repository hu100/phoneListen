package com.phone.listen.util;

import android.os.Environment;

public class Constant {

    public static final String LOG_PATH = Environment.getExternalStorageDirectory().getAbsolutePath() + "/1";
}
